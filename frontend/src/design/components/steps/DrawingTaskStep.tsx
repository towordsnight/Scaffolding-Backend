/**
 * Step 9: no input control — the student does the work on the right-hand
 * scratchpad, then hits "Check Drawing". Renders nothing between the
 * prompt and the action button.
 */
export function DrawingTaskStep() {
  return null;
}
